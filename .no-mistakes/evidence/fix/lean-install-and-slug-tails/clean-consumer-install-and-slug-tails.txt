pi-worklist clean-consumer acceptance transcript

$ npm install <locally packed pi-worklist tarball>

added 5 packages, and audited 6 packages in 1s

found 0 vulnerabilities

$ inspect installed footprint and peer absence
node_modules total: 816 KiB
pi-worklist package: 568 KiB
PASS: node_modules/@earendil-works is absent
installed top-level directories:
.bin
graceful-fs
pi-worklist
proper-lockfile
retry
signal-exit

$ installed pi-worklist project add "Add pi-orchestrator compatibility and cross-extension E2E tests"
Added project goal add-pi-orchestrator-compatibility-and: Add pi-orchestrator compatibility and cross-extension E2E tests

$ installed pi-worklist project add "Keep project migration safe checks off during rollout"
Added project goal keep-project-migration-safe-checks-off: Keep project migration safe checks off during rollout

$ installed pi-worklist project list
[open] add-pi-orchestrator-compatibility-and: Add pi-orchestrator compatibility and cross-extension E2E tests
[open] keep-project-migration-safe-checks-off: Keep project migration safe checks off during rollout

$ persisted goal IDs from .pi/worklist.json
add-pi-orchestrator-compatibility-and :: Add pi-orchestrator compatibility and cross-extension E2E tests
keep-project-migration-safe-checks-off :: Keep project migration safe checks off during rollout
