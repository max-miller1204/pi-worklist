Compiled peer-import scanner negative control
=============================================

Temporary injected source line:
  import * as injectedPeer from "@earendil-works/pi-tui";

Focused command:
  vitest run test/compiled-cli.test.ts -t 'imports only declared dependencies'

Expected failure observed:
  AssertionError: the compiled bin must not depend on an uninstalled peer
  Received:
    dist/git.js imports @earendil-works/pi-tui
  negative_control_exit=1

The temporary import was removed and the complete compiled-cli test file was
rerun successfully, rebuilding dist without the injected dependency.
