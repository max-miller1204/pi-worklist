pi-worklist description-flag end-to-end transcript
worktree:      /home/max/.no-mistakes/worktrees/9f1c7bdf32ed/01KZA9BRAEKXSK9BZCGJJ92Y3N
base commit:   88c385038ce12f7a5a68fe203ccfd20348dadf60
target commit: 29541b8172630ee44003a7da2a959564c0b446b0
node:          v24.18.1
Commands below are run as `node src/cli.ts <args>`; shown as `pi-worklist <args>`.

============================================================
1. The ordering hazard, BEFORE the change (base commit 88c3850)
============================================================
Identical argv, run against the base-commit CLI checked out to /tmp/pi-worklist-base.
A trailing --json is swallowed into the description: exit 0, human stdout, goal written.

$ pi-worklist project add Ship it properly -- External agent access --json
--- stdout ---
Added project goal ship-it-properly: Ship it properly
--- stderr ---
Warning: --json came after -- and became description text, not a flag.
Flags must come before the -- separator, for example:
  pi-worklist project <action> [arguments] --json -- <description>
--- exit code: 0 ---

[stored .pi/worklist.json goals]
  title:       "Ship it properly"
  description: "External agent access --json"
  ---

============================================================
2. The same invocation AFTER the change (target commit 29541b8)
============================================================
Now a strict usage error with exit code 2, no stdout, and nothing written.

$ pi-worklist project add Ship it properly -- External agent access --json
--- stderr ---
--json came after --, where known flags are not accepted as description text.
Use --description <text> to replace, or --append-description <text> to add, a description that contains a standalone flag; reserve -- for interactive prose.

Usage: pi-worklist project <action> [arguments] [flags]

Actions:
  list                                     Show a compact bounded list of project goals
  show <id>                                Show one goal with its full description
  find <text...>                           List the goals whose title or description contains the text
  ui                                       Open the interactive goal board for a human at the keyboard
  add <title...> [--description <text> | -- <description...>]
                                           Add an open goal
  update <id> [title...] [--description <text> | -- <description...>]
                                           Edit a goal's title or description
  move <id> up|down|before <id>|after <id> Reorder a goal in the roadmap's canonical file order
  set_active <id>                          Make a goal the single active goal
  complete <id> --confirm                  Mark a goal done
  reopen <id> --confirm                    Reopen a done or archived goal
  archive <id> --confirm                   Archive a goal
  delete <id> --confirm                    Delete a goal permanently
  migrate_ids --confirm                    Rewrite randomly generated goal IDs as title-derived ones
  help                                     Print this help

Flags:
  --json                           Print the deterministic result envelope as JSON (stdout on success, stderr on failure)
  --confirm                        Acknowledge an action that requires confirmation; pass it only for an explicit user request
  --cwd <dir>                      Resolve the git root from this directory instead of the working directory
  --description <text>             Set the whole description from one argv token; order-independent and preferred for agents and scripts; a new update title must come before it, and an add title must not straddle it; only for project add and update
  --append-description <text>      Add one argv token as a new description paragraph without replacing stored prose; cannot be combined with a title change; only for project update
  --append                         Interactive compatibility form that adds the text after -- as a new paragraph; cannot be combined with a title change; only for project update
  --group <name>                   Put the goal in a free-form section, such as Foundation; an empty name clears it; only for project add and update
  --depends-on <id>                Require that goal to land first; repeat it to name several, and pass an empty id alone to clear every edge; only for project add and update
  --expect-updated-at <timestamp>  Refuse the change as a conflict unless the goal's updatedAt still matches this value; only for project update, set_active, complete, reopen, archive, and delete
  --dry-run                        Report the ID rewrites without writing them, and without needing --confirm; only for project migrate_ids

Exit codes: 0 success, 1 error, 2 usage error, 3 confirmation required, 4 conflict.
--- exit code: 2 ---

[stored .pi/worklist.json goals]
  (no worklist file yet)

============================================================
3. --description: order-independent, whole value in one argv token
============================================================

[3a] Title first, description value that itself looks like a known flag.

$ pi-worklist project add Flag-safe goal --description --confirm
--- stdout ---
Added project goal flag-safe-goal: Flag-safe goal
--- exit code: 0 ---

[stored .pi/worklist.json goals]
  title:       "Flag-safe goal"
  description: "--confirm"
  ---

[3b] Same command with the flag written FIRST (order independence).

$ pi-worklist project add --description "Prose written first" Second goal
--- stdout ---
Added project goal second-goal: Second goal
--- exit code: 0 ---

[stored .pi/worklist.json goals]
  title:       "Flag-safe goal"
  description: "--confirm"
  ---
  title:       "Second goal"
  description: "Prose written first"
  ---

============================================================
4. Replacement, append, and clear through the new flags
============================================================

$ pi-worklist project add Ship the parser --description "Original prose"
--- stdout ---
Added project goal ship-the-parser: Ship the parser
--- exit code: 0 ---

[resolved goal id: ship-the-parser]

[4a] Direct replacement, flag written before the id.

$ pi-worklist project update --description "Replacement text mentions --json" ship-the-parser
--- stdout ---
Updated project goal ship-the-parser
--- exit code: 0 ---

[stored .pi/worklist.json goals]
  title:       "Ship the parser"
  description: "Replacement text mentions --json"
  ---

[4b] Append a paragraph without replaying the stored prose.

$ pi-worklist project update ship-the-parser --append-description "Blocked on the template schema until it lands"
--- stdout ---
Updated project goal ship-the-parser
--- exit code: 0 ---

[stored .pi/worklist.json goals]
  title:       "Ship the parser"
  description: "Replacement text mentions --json\n\nBlocked on the template schema until it lands"
  ---

[4c] Replace the title and the description in one write.

$ pi-worklist project update ship-the-parser Renamed goal --description "Replacement prose"
--- stdout ---
Updated project goal ship-the-parser
--- exit code: 0 ---

[stored .pi/worklist.json goals]
  title:       "Renamed goal"
  description: "Replacement prose"
  ---

[4d] Clear the description programmatically.

$ pi-worklist project update ship-the-parser --description ""
--- stdout ---
Updated project goal ship-the-parser
--- exit code: 0 ---

[stored .pi/worklist.json goals]
  title:       "Renamed goal"
  description: ""
  ---

============================================================
5. Backwards compatibility: the interactive -- separator and legacy --append
============================================================

[5a] A human types unquoted prose after the separator.

$ pi-worklist project add Retire the importer -- Prose a human typed
--- stdout ---
Added project goal retire-the-importer: Retire the importer
--- exit code: 0 ---

[stored .pi/worklist.json goals]
  title:       "Retire the importer"
  description: "Prose a human typed"
  ---

[5b] Legacy --append -- <text> interactive form still appends.

$ pi-worklist project update retire-the-importer --append -- Blocked on the schema
--- stdout ---
Updated project goal retire-the-importer
--- exit code: 0 ---

[stored .pi/worklist.json goals]
  title:       "Retire the importer"
  description: "Prose a human typed\n\nBlocked on the schema"
  ---

[5c] Title plus separator description in one interactive write.

$ pi-worklist project update retire-the-importer Renamed again -- Separator prose
--- stdout ---
Updated project goal retire-the-importer
--- exit code: 0 ---

[stored .pi/worklist.json goals]
  title:       "Renamed again"
  description: "Separator prose"
  ---

[5d] `update <id> --` alone still clears the description.

$ pi-worklist project update retire-the-importer --
--- stdout ---
Updated project goal retire-the-importer
--- exit code: 0 ---

[stored .pi/worklist.json goals]
  title:       "Renamed again"
  description: ""
  ---

============================================================
6. Strict, no-mutation usage errors (exit code 2)
============================================================

$ pi-worklist project add Ship it --description "Original prose"
--- stdout ---
Added project goal ship-it: Ship it
--- exit code: 0 ---

[6a] A standalone known flag after -- names both escape hatches.

$ pi-worklist project update ship-it --append -- Blocked until --json lands
--- stderr ---
--json came after --, where known flags are not accepted as description text.
Use --description <text> to replace, or --append-description <text> to add, a description that contains a standalone flag; reserve -- for interactive prose.
[usage block omitted - shown in full in section 2]
--- exit code: 2 ---

[stored .pi/worklist.json goals]
  title:       "Ship it"
  description: "Original prose"
  ---

[6b] Several misplaced flags are all named, with plural wording.

$ pi-worklist project add Count the flags -- Prose --json --confirm
--- stderr ---
--json, --confirm came after --, where known flags are not accepted as description text.
Use --description <text> to replace, or --append-description <text> to add, a description that contains standalone flags; reserve -- for interactive prose.
[usage block omitted - shown in full in section 2]
--- exit code: 2 ---

[stored .pi/worklist.json goals]
  title:       "Ship it"
  description: "Original prose"
  ---

[6c] Unquoted prose spilling past a --description value would have renamed the goal.

$ pi-worklist project update ship-it --description Some long prose
--- stderr ---
project update reads the words after a --description value as a new title, so this would rename the goal.
Write a new title before --description, and pass the whole description as one argument.
[usage block omitted - shown in full in section 2]
--- exit code: 2 ---

[stored .pi/worklist.json goals]
  title:       "Ship it"
  description: "Original prose"
  ---

[6d] Unquoted prose spilling past an --append-description value.

$ pi-worklist project update ship-it --append-description Blocked on parser
--- stderr ---
project update reads the words after an --append-description value as a new title, so this would rename the goal.
Pass the whole note as one argument; appending never changes the title.
[usage block omitted - shown in full in section 2]
--- exit code: 2 ---

[stored .pi/worklist.json goals]
  title:       "Ship it"
  description: "Original prose"
  ---

[6e] Replacing and appending remain mutually exclusive.

$ pi-worklist project update ship-it --description replace --append-description add
--- stderr ---
--description and --append-description are mutually exclusive
[usage block omitted - shown in full in section 2]
--- exit code: 2 ---

[6f] Appending still cannot be combined with a title change.

$ pi-worklist project update ship-it Renamed --append-description "A note"
--- stderr ---
project update cannot change the title while appending to the description
[usage block omitted - shown in full in section 2]
--- exit code: 2 ---

[6g] --description cannot be combined with separator text.

$ pi-worklist project add Nope --description first -- second
--- stderr ---
--description cannot be combined with description text after --
[usage block omitted - shown in full in section 2]
--- exit code: 2 ---

[stored .pi/worklist.json goals]
  title:       "Ship it"
  description: "Original prose"
  ---

============================================================
7. JSON application failures stay parseable with a flag-looking value
============================================================

$ pi-worklist project update goal-missing --description --confirm --json
--- stderr ---
{
  "ok": false,
  "scope": "project",
  "action": "update",
  "error": {
    "code": "NOT_FOUND",
    "message": "Project goal goal-missing was not found.",
    "retryable": false,
    "details": {
      "entity": "project-goal",
      "id": "goal-missing",
      "resolution": "refresh-and-select-existing"
    }
  },
  "meta": {
    "changed": false,
    "semanticNoOp": false,
    "changedFields": [],
    "cliVersion": "0.14.1"
  }
}
--- exit code: 1 ---

[stderr parsed as JSON by an independent reader]
  ok:            false
  error.code:    NOT_FOUND
  error.message: Project goal goal-missing was not found.
  warnings key present: false

============================================================
8. Generated caller surfaces point programmatic callers at the new flags
============================================================

[docs/cli.md -> Description input section]
## Description input

Programmatic callers and agents must use `--description <text>` for a replacement, passing the whole value in one argv token. The flag is order-independent, and its value may itself look like a known flag.
Write a new title before `--description` rather than after its value: on `update`, a trailing word that would become a title is refused with exit code 2 instead of renaming the goal, and on `add` a title split across the value is refused the same way. `add` otherwise folds words after the value into the title rather than failing, so quote the whole description there. `update <id> <title...> --description <text>` still replaces a title and a description at once.
Use `--append-description <text>` to add a paragraph without replaying stored prose. Replacing and appending are mutually exclusive, and an append cannot be combined with a title change.
Reserve `-- <description...>` for a human typing unquoted prose interactively. A standalone known flag after the separator is a usage error with exit code 2; move a real flag before the separator or put flag-looking prose in `--description`.
The legacy `--append -- <text>` interactive form remains supported, while agents and scripts use `--append-description <text>`.
Programmatic callers clear a description with `--description ''`; the interactive `update <id> --` form remains supported.

## Goal IDs

[.claude/skills/worklist/SKILL.md -> Examples block]
Examples:

```sh
npx -y pi-worklist@latest project list --json
npx -y pi-worklist@latest project add Support goal templates --description "Let teams share reusable goal outlines"
npx -y pi-worklist@latest project find templates --json
npx -y pi-worklist@latest project show support-goal-templates --json
npx -y pi-worklist@latest project update support-goal-templates --description "Replace only the description"
npx -y pi-worklist@latest project update support-goal-templates Support shared goal templates
npx -y pi-worklist@latest project update support-goal-templates --append-description "Blocked on the template schema until it lands"
npx -y pi-worklist@latest project update support-goal-templates --expect-updated-at 2026-05-04T09:12:31.004Z --append-description "Reviewed and still current"
npx -y pi-worklist@latest project update support-goal-templates --group Foundation

[end of transcript]
